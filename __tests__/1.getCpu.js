import React from "react"

describe('getCpu function', () => {
	it('if input has the cpu field, and is valid value return celsius', () => {

	})

	it(`if input doesn't have the cpu field return null`, () => {

	})

	it(`if input doesn't have a valid value return null`, () => {

	})

	it('should succeed, and fail against a few use cases (the cpu value should be a number, between -20C to 60C)', () => {

	})
})